#include <stdio.h>
#include <stdlib.h>


struct node {
    int val;
    struct node* left;
    struct node* right;
};

struct node* construct(int* preoder, int* inorder, int n) {

}

int main() {
    return 0;
}
